{
  "name": "cirrus-weather-dashboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A keyless, accessible weather dashboard powered by Open-Meteo.",
  "scripts": {
    "test": "node --test",
    "check:syntax": "node --check assets/js/weather.js && node --check assets/js/api.js && node --check assets/js/storage.js && node --check assets/js/script.js && node --check tests/weather.test.js && node --check tests/api.test.js && node --check tests/markup.test.js",
    "check": "npm run check:syntax && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
